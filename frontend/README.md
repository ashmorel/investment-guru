# Investment Guru — frontend

React + TypeScript + Vite web app for the Investment Guru portfolio tracker.

Run locally: `npm install && npm run dev` (proxies `/api` to the backend on :8000).

Full gate before pushing: `npm run check` (tsc + lint + tests + build).
